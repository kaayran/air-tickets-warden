# @telegram-apps/telegram-ui@2.x declares a peer of react@^18, but the project
# runs React 19 (verified working). Accept the peer mismatch so npm ci is
# consistent across local, Docker, and CI. Revisit when telegram-ui ships
# React 19 peers.
legacy-peer-deps=true
