{
  "mcpServers": {
    "warden-postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "${WARDEN_MCP_DATABASE_URL}"
      ]
    }
  }
}
